{
  "name": "answer-grammar",
  "version": "0.1.0",
  "private": true,
  "description": "Single grammar for parsing a solve model's ### GROUPS / ### ANSWER response text, shared by the orchestrator and backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
